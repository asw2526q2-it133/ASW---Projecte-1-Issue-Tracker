openapi: 3.0.0
info:
  title: Taiga API (ASW Project)
  description: API REST per al gestor d'incidències.
  version: 1.0.0

servers:
  - url: http://localhost:10000/api
    description: Servidor Local
  # Afegeix aquí el de Render quan el tinguis, ex: url: https://la-teva-app.onrender.com/api

components:
  securitySchemes:
    ApiKeyAuth:
      type: apiKey
      in: header
      name: X-Api-Key

  schemas:
    # --- NOU: Esquemes per a Comentaris i Activitats (US90) ---
    Comment:
      type: object
      properties:
        id:
          type: integer
          example: 1
        body:
          type: string
          example: "He revisat això i segueix fallant."
        user_id:
          type: integer
          example: 2
        created_at:
          type: string
          format: date-time
          
    Activity:
      type: object
      properties:
        id:
          type: integer
          example: 10
        description:
          type: string
          example: "L'usuari ha canviat l'estat a In Progress"
        created_at:
          type: string
          format: date-time
    # ---------------------------------------------------------

    Issue:
      type: object
      properties:
        id:
          type: integer
          example: 1
        subject:
          type: string
          example: "Afegir botó de login"
        description:
          type: string
          example: "Cal afegir un botó al menú principal..."
        status_id:
          type: integer
          example: 2
        user_id:
          type: integer
          example: 5
        created_at:
          type: string
          format: date-time
        # --- NOU: Afegim les llistes a la Issue (US90) ---
        comments:
          type: array
          items:
            $ref: '#/components/schemas/Comment'
        activities:
          type: array
          items:
            $ref: '#/components/schemas/Activity'
    # -------------------------------------------------
    IssueType:
      type: object
      properties:
        id:
          type: integer
          example: 1
        name:
          type: string
          example: "Bug"
        color:
          type: string
          example: "#ff0000"
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
security:
  - ApiKeyAuth: []

paths:
  /issues:
    get:
      summary: Llista totes les issues
      description: Retorna un array amb totes les issues del sistema en format JSON.
      tags:
        - Issues
      responses:
        '200':
          description: Operació exitosa.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Issue'
        '401':
          description: No autoritzat. Cal una API Key.
          
    post:
      summary: Crea una nova issue
      description: Afegeix una nova incidència al sistema. Fa servir multipart/form-data per permetre pujar fitxers des de l'editor.
      tags:
        - Issues
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                'issue[subject]':
                  type: string
                  description: Títol de la issue (Obligatori)
                  example: "S'ha trencat el botó de login"
                'issue[description]':
                  type: string
                  description: Descripció detallada
                  example: "Quan intento fer login, la pàgina dóna error 500."
                'issue[issue_type_id]':
                  type: integer
                  example: 1
                'issue[severity_id]':
                  type: integer
                  example: 1
                'issue[priority_id]':
                  type: integer
                  example: 1
                'issue[status_id]':
                  type: integer
                  example: 1
                'issue[attachments][]':
                  type: array
                  items:
                    type: string
                    format: binary
                  description: "Pots seleccionar i pujar diversos fitxers aquí"
      responses:
        '201':
          description: Issue creada correctament.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Issue'
        '401':
          description: No autoritzat. Api-Key invàlida o absent.
        '422':
          description: Error de validació (ex. falta el subject).

  /issues/{id}:
    get:
      summary: Obté una issue concreta
      description: Retorna les dades d'una incidència concreta incloent els seus comentaris i activitats (Tasques de la US90).
      tags:
        - Issues
      parameters:
        - name: id
          in: path
          required: true
          description: L'ID de la incidència que volem consultar.
          schema:
            type: integer
            example: 1
      responses:
        '200':
          description: Operació exitosa. Retorna la informació de la issue.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Issue'
        '401':
          description: No autoritzat. Api-Key invàlida o absent.
        '404':
          description: Error. La issue sol·licitada no existeix.
  #----------------------------------------------------------
  /issue_types:
    get:
      summary: Llista tots els tipus d'incidència
      description: Retorna un array amb tots els tipus d'incidència disponibles al sistema.
      tags:
        - Issue Types
      responses:
        '200':
          description: Operació exitosa.
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/IssueType'
        '401':
          description: No autoritzat. Cal una API Key.
    post:
      summary: Crea un nou tipus d'incidència
      description: Afegeix un nou tipus (ex. Bug, Question) al sistema.
      tags:
        - Issue Types
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                issue_type:
                  type: object
                  properties:
                    name:
                      type: string
                      description: Nom del tipus (Obligatori i únic)
                      example: "Enhancement"
                    color:
                      type: string
                      description: Color en format HEX
                      example: "#3498db"
      responses:
        '201':
          description: Tipus creat correctament.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IssueType'
        '401':
          description: No autoritzat. Api-Key invàlida o absent.
        '422':
          description: Error de validació (ex. nom duplicat o buit).

  /issue_types/{id}:
    get:
      summary: Obté un tipus concret
      description: Retorna les dades d'un tipus d'incidència segons el seu ID.
      tags:
        - Issue Types
      parameters:
        - name: id
          in: path
          required: true
          description: L'ID numèric del tipus.
          schema:
            type: integer
            example: 1
      responses:
        '200':
          description: Operació exitosa.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IssueType'
        '401':
          description: No autoritzat. Api-Key invàlida o absent.
        '404':
          description: Tipus no trobat.

    patch:
      summary: Actualitza un tipus
      description: Modifica el nom o el color d'un tipus existent usant el seu ID numèric.
      tags:
        - Issue Types
      parameters:
        - name: id
          in: path
          required: true
          description: L'ID numèric del tipus que vols modificar.
          schema:
            type: integer
            example: 1
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                issue_type:
                  type: object
                  properties:
                    name:
                      type: string
                      example: "Bug Crític"
                    color:
                      type: string
                      example: "#ff0000"
      responses:
        '200':
          description: Tipus actualitzat correctament.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/IssueType'
        '401':
          description: No autoritzat. Api-Key invàlida o absent.
        '404':
          description: Tipus no trobat.
        '422':
          description: Error de validació.

    delete:
      summary: Esborra un tipus
      description: Elimina un tipus del sistema. Si està en ús, cal enviar el paràmetre `replacement_id` per reassignar les issues. No es pot esborrar si només en queda un.
      tags:
        - Issue Types
      parameters:
        - name: id
          in: path
          required: true
          description: L'ID numèric del tipus que vols esborrar.
          schema:
            type: integer
            example: 1
        - name: replacement_id
          in: query
          required: false
          description: L'ID del tipus que heretarà les issues (Obligatori si el tipus que s'esborra està en ús).
          schema:
            type: integer
            example: 2
      responses:
        '204':
          description: Tipus eliminat correctament (Sense contingut).
        '401':
          description: No autoritzat. Api-Key invàlida o absent.
        '404':
          description: Tipus o replacement_id no trobat.
        '422':
          description: Error. Falta el replacement_id, s'està intentant esborrar l'últim registre, etc.